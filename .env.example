# PixelGroomer Configuration
# Copy this file to .env and adjust to your needs
#
# Configuration Priority (highest to lowest):
#   1. CLI arguments (--event, --location, etc.)
#   2. This .env file
#   3. .import.yaml on SD card
#
# CLI always wins, .env overrides SD card defaults.

# =============================================================================
# Paths
# =============================================================================
PHOTO_LIBRARY="$HOME/Pictures/PhotoLibrary"
ALBUM_DIR="$HOME/Pictures/Albums"
EXPORT_DIR="$HOME/Pictures/Export"

# =============================================================================
# Folder Structure
# =============================================================================
# Pattern for organizing photos. Available placeholders:
#   {year}  - 4-digit year (2026)
#   {month} - 2-digit month (01-12)
#   {day}   - 2-digit day (01-31)
#
# Examples:
#   {year}-{month}-{day}        -> 2026-01-24/
#   {year}/{month}/{day}        -> 2026/01/24/
#   {year}-{month}/{day}        -> 2026-01/24/
#   {year}/{year}-{month}-{day} -> 2026/2026-01-24/
FOLDER_STRUCTURE="{year}-{month}-{day}"

# =============================================================================
# Import Behavior
# =============================================================================
# Prompt for archive directory on each import (true/false)
# If false, uses PHOTO_LIBRARY directly
PROMPT_ARCHIVE_DIR="false"

# =============================================================================
# Default Metadata
# =============================================================================
DEFAULT_AUTHOR="Your Name"
DEFAULT_COPYRIGHT="© 2026 Your Name"

# =============================================================================
# Naming Pattern
# =============================================================================
# Available placeholders:
#   {date}     - YYYYMMDD from EXIF DateTimeOriginal
#   {time}     - HHMMSS from EXIF DateTimeOriginal
#   {event}    - Event name (from CLI, .import.yaml, or prompt)
#   {seq}      - Sequence number (use {seq:03d} for zero-padding)
#   {camera}   - Camera model from EXIF
#   {ext}      - Original file extension (lowercase)
NAMING_PATTERN="{date}_{event}_{seq:03d}"

# =============================================================================
# RAW Development
# =============================================================================
# Processor: darktable | imagemagick
RAW_PROCESSOR="darktable"
JPEG_QUALITY=92

# Default darktable style/preset (leave empty for no style)
DARKTABLE_STYLE=""

# =============================================================================
# Supported Formats
# =============================================================================
# RAW extensions (comma-separated, case-insensitive matching is done internally)
RAW_EXTENSIONS="cr2,cr3,nef,arw,raf,dng,orf,rw2"

# Image extensions for import
IMAGE_EXTENSIONS="jpg,jpeg,png,tiff,tif,heic,heif"

# =============================================================================
# Behavior
# =============================================================================
# Ask before deleting source files after import (true/false)
CONFIRM_DELETE="true"

# Generate checksums during import (true/false)
GENERATE_CHECKSUMS="true"

# Checksum algorithm (sha256/md5)
CHECKSUM_ALGORITHM="sha256"
